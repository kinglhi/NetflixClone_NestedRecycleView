package com.example.netflixclone_recyclerview

import android.widget.ImageView
import androidx.annotation.DrawableRes

class ChildItem(
    // declaration of the mutable variable
    @DrawableRes val childItemImage: Int
    //  no image variable because we declared it in the child_item layout file
)